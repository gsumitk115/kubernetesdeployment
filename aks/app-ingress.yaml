apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: webapp-ingress
  namespace: app
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: svc-aks
          servicePort: 80
             
  - host: frontend.20.99.176.173.nip.io # change the IP address here
    http:
      paths:
      - path: /
        backend:
          serviceName: svc-aks
          servicePort: 80
        
      